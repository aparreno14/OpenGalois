# OpenGalois v3 Fixtures

This directory contains the test certificates for the v3 architecture (Facts/Rules), organized by *ruleset*.

During the migration, Continuous Integration (CI) will automatically validate any `.json` file added here exclusively against the `3.0.0.json` JSON Schema. Full mathematical verification will be enabled once the v3 verifier is implemented.