# Certificate semantics (v2.0.0)

This directory is expected to contain the normative semantics document:

- `schema-v2.0.0.md`

In the repository, keep this file aligned with `schemas/certificate/2.0.0.json`.
