## Review guidelines

- Ignore files under `legacy/` during pull request review.
- Do not leave review comments on `legacy/**`.
- Only comment if a change introduces a critical security issue or a guaranteed build-breaking issue outside `legacy/`.