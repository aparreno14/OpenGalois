id: degree.QQ@1
ruleset: le5-core@1

claim_pattern:
  pred: Degree
  args: [PolyQQ, IntZ]

premise_patterns: []

evidence_schema: null

check_mode: recompute

checker:
  steps:
    - "Decode f canonically as a PolyQQ."
    - "Decode n canonically as an IntZ."
    - "Recompute deg(f) using canonical polynomial decoding (trim leading zeros)."
    - "Accept iff n == deg(f)."
errors:
  - E_TYPE
  - E_MISMATCH
